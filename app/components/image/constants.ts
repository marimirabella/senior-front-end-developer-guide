import { CSSProperties } from 'react';

export const imageStyles: CSSProperties = { objectFit: 'contain' };

export const defaultImageSizes =
  '(max-width: 900px) 100vw, (max-width: 1200px) 50vw, 30vw';
