import { UseScrollTriggerOptions } from '@mui/material/useScrollTrigger/useScrollTrigger';

export const useScrollTriggerOptions: UseScrollTriggerOptions = {
  threshold: 300,
};

export const scrollToOptions: ScrollToOptions = {
  top: 0,
  behavior: 'smooth',
};
